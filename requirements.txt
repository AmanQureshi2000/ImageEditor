# Core requirements with compatible versions
PyQt5>=5.15.0
Pillow>=9.0.0
numpy==1.24.3  # Pin to NumPy 1.x for OpenCV compatibility
opencv-python==4.8.1.78  # Specific version known to work
scikit-image==0.21.0  # Compatible with NumPy 1.24

# Optional AI libraries (comment out if not needed)
onnxruntime>=1.14.0
torch>=2.0.0
torchvision>=0.15.0